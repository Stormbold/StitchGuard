# Claude Code Workflow

```bash
stitchguard check \
  --target ./design/mobile.png \
  --url http://localhost:3000 \
  --agent claude \
  --mode conservative
```

The Claude prompt prioritizes layout spacing, color tokens, typography, border radius, and bottom navigation padding.
