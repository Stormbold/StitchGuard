# Good First Issues

Contributions welcome. Pick an issue that matches your experience.

## CLI

- Add JPEG input support in `@stitchguard/core`
- Add `--output-dir` validation and clearer error messages
- Add `--full-page` flag documentation for `check`

## Reports

- Add JSON schema validation for `result.json`
- Add HTML report export (optional, behind flag)

## Examples

- Add a new example to `examples/` with before/after story
- Add one `bad-ui-zoo/` case with README

## Action

- Improve PR comment formatting with collapsible prompt section
- Add badge snippet for README (score shield)

## Docs

- Add dark-mode README screenshot
- Translate quickstart to German (optional)

## Testing

- Add CLI integration test for `init-agent`
- Add MCP server tool smoke test

Open an issue before large changes. Run `pnpm test` and `pnpm lint` before submitting a PR.
