# Good First Issues

Pick an issue below and comment before large changes. Run `pnpm test` and `pnpm lint` before submitting a PR.

## Launch backlog (create on GitHub)

These should exist as real Issues — copy titles/descriptions from this list:

| Title | Labels |
|-------|--------|
| Add npm publishing workflow for `stitchguard` CLI | enhancement |
| Add copy button for repair prompt in web demo | enhancement, **good first issue** |
| Document GitHub Action release tags (`v1` vs semver) | documentation |
| Add Cursor workflow example to `examples/` | enhancement |
| Add Claude Code workflow example to `examples/` | enhancement |
| Add Tailwind hint confidence levels | enhancement |
| Add ignore regions to web demo | enhancement, **good first issue** |
| Add sample PR comment screenshot to docs | documentation, **good first issue** |
| Add Storybook integration example | enhancement |
| Add CLI smoke test in CI | testing |
| Record README demo GIF (`docs/assets/stitchguard-demo.gif`) | documentation |
| Fill GitHub About/Topics (see `docs/github-about.md`) | documentation |

## CLI

- Add JPEG input support in `@stitchguard/core`
- Add `--output-dir` validation and clearer error messages

## Reports

- Add JSON schema validation for `result.json`
- Add HTML report export (optional, behind flag)

## Examples

- Add a new example to `examples/` with before/after story
- Add one `bad-ui-zoo/` case with README

## Action

- Add badge snippet for README (score shield)

## Testing

- Add CLI integration test for `init-agent`
- Add MCP server tool smoke test

Open an issue before large changes.
